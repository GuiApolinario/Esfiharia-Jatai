/* =============================================================================
   CONFIGURAÇÃO DO SITE — preencha as duas linhas abaixo uma única vez.

   Onde encontrar esses valores:
     1. Entre no painel do Supabase (https://supabase.com/dashboard)
     2. Abra o seu projeto
     3. Vá em  Project Settings  >  API Keys
     4. Copie "Project URL" e a chave pública "anon"

   Pode publicar esses dois valores sem medo: a chave "anon" é pública de
   propósito e só permite ler o cardápio. Quem manda no que pode ser alterado
   são as permissões configuradas no banco (arquivo supabase/schema.sql).

   NUNCA coloque aqui a chave "service_role" — essa é secreta.
   ========================================================================== */

export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
